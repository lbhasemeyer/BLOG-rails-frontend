<div class="all-posts">

  {{#each post in model}}
    <div class="a-post">
      {{#link-to 'post' post}}
        <h2>{{post.title}}</h2>
      {{/link-to}}
        <button {{action 'deletePost' post.id}}>Delete Note</button>
    </div>
  {{/each}}
</div>


{{outlet}}
